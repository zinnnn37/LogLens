############################################################
#  Fluent Bit 설정 파일 (Spring Boot + Front 로그 통합 수집)
#  - 백엔드(/logs/be)와 프론트엔드(/logs/fe) 로그를 동시에 수집
#  - 공통 필터로 구조 통합 후 Kafka(application-logs 토픽)으로 전송
#  - 최종적으로 BE/FE는 source_type 필드로 구분
############################################################

[SERVICE]
# ──────────────────────────────────────────────
# 🔧 Fluent Bit 기본 서비스 설정
# ──────────────────────────────────────────────
Flush 5
Daemon Off
Log_Level info
Parsers_File /fluent-bit/etc/parsers.conf

# ✅ 모니터링용 HTTP 서버 설정 (상태/메트릭 확인용)
HTTP_Server On
HTTP_Listen 0.0.0.0
HTTP_Port 2020

# ✅ 환경 변수 등록 (record_modifier에서 참조)
Env PROJECT_UUID


############################################################
# INPUT 단계: 로그 파일을 읽어오는 부분
############################################################
# ===========================================
# INPUT 1: 백엔드 로그 (/logs/be/*.log)
# ===========================================
[INPUT]
Name tail
Path /logs/be/*.log
Tag app.logs
Refresh_Interval 5
Read_from_Head true
Skip_Long_Lines On
Mem_Buf_Limit 10MB
DB /var/log/fluentbit-be-offsets.db

# ===========================================
# INPUT 2: 프론트엔드 로그 (/logs/fe/*.log)
# ===========================================
[INPUT]
Name tail
Path /logs/fe/*.log
Tag app.logs
Refresh_Interval 5
Read_from_Head true
Skip_Long_Lines On
Mem_Buf_Limit 10MB
DB /var/log/fluentbit-fe-offsets.db


############################################################
# FILTER 단계: 로그 가공 및 구조 정리
############################################################
# ===========================================
# FILTER 1: JSON 파싱 (이미 JSON 구조인 로그 처리)
# ===========================================
[FILTER]
Name parser
Match app.logs
Key_Name log
Parser json
Reserve_Data On
Preserve_Key On

# ===========================================
# FILTER 2: 텍스트 로그 파싱 (Spring Boot 텍스트 로그 처리)
# ===========================================
[FILTER]
Name parser
Match app.logs
Key_Name log
Parser spring_boot_text
Reserve_Data On
Preserve_Key On

# ===========================================
# FILTER 3: 메타데이터 추가 (프로젝트 UUID)
# ===========================================
[FILTER]
Name record_modifier
Match app.logs
Record project_uuid $ {
    PROJECT_UUI
}

# source_type 자동 구분 (파일 경로 기반: BE/FE/INFRA)
[FILTER]
Name lua
Match app.logs
script /fluent-bit/scripts/transform.lua
call detect_source_type

# ===========================================
# FILTER 4: 공통 필드 정리
# ===========================================
[FILTER]
Name modify
Match app.logs
Copy level log_level
Copy package logger
Copy @timestamp timestamp

# ===========================================
# FILTER 5: Lua 스크립트 변환 (OpenSearch 스키마 변환)
# ===========================================
[FILTER]
Name lua
Match app.logs
script /fluent-bit/scripts/transform.lua
call transform_log


############################################################
# OUTPUT 단계: 외부 시스템으로 로그 전송
############################################################
# ===========================================
# OUTPUT: Kafka 전송 (공통 토픽)
# ===========================================
[OUTPUT]
Name kafka
Match app.logs
Brokers ai.loglens.store:9092
Topics application-logs
Format json
Timestamp_Key timestamp
Timestamp_Format iso8601

# ─ Kafka 고급 설정 ─
rdkafka.request.required.acks 1
rdkafka.message.timeout.ms 10000
rdkafka.retry.backoff.ms 100
rdkafka.socket.keepalive.enable true
rdkafka.log.connection.close false
rdkafka.batch.size 16384
rdkafka.batch.num.messages 1000
rdkafka.compression.type snappy
rdkafka.request.timeout.ms 30000
rdkafka.metadata.max.age.ms 180000

# ===========================================
# OUTPUT: 디버깅용 (stdout)
# ===========================================
[OUTPUT]
Name stdout
Match app.logs
Format json_lines
