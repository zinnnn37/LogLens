version: '3.8'

services:
  ai-service-green:
    image: ai-service:latest
    container_name: ai-service-green
    ports:
      - "8001:8000"
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    environment:
      - SLOT=green
      - UVICORN_PORT=8000
      - LOG_LEVEL=INFO
